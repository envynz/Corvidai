[build]
  command = "vite build && npx playwright install --with-deps chromium && node scripts/prerender.mjs"
  publish = "dist/public"
  functions = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
